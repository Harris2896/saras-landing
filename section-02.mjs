import fs from 'node:fs/promises';let s=await fs.readFile('dist/index.html','utf8');s=s.replace('<section class="collection',await fs.readFile('sections/02-problems.html','utf8')+'<section class="collection');const a=s.indexOf('<section class="care-section">');const b=s.indexOf('<section class="ritual',a);if(a>=0&&b>a)s=s.slice(0,a)+s.slice(b);await fs.writeFile('dist/index.html',s);
